<h1><%= current_user.username %>, below you can edit your review</h1>

<form method="POST" action="/reviews/<%= @review.id %>">
<input type="hidden" id="hidden" name="_method" value="PATCH">
  Confirm that the correct Sight is checked:<br>
  <% @sights.each do |sight| %>
    <input type="checkbox" name="sight[id]" id="sight_<%= sight.id %>" value="<%= sight.id %>"<%='checked' if @sight.id == sight.id %>><%= sight.name %><br>
  <% end %>
  <br></br>
  <textarea name="review[content]" rows="4" cols="50" id="
  review_content"><%= @review.content %></textarea>
  <br></br>
  <input type="submit" value="Submit Edited Review">
</form>